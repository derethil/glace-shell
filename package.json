{
  "name": "glace-shell",
  "version": "0.1.0",
  "dependencies": {
    "fzf": "^0.5.2"
  },
  "scripts": {
    "postinstall": "ln -s /usr/share/astal/gjs ./node_modules/astal || true",
    "init": "ags types -d .; npm install",
    "dev": "ags run -d .",
    "dev:watch": "watchexec -r -w . -- ags run -d ."
  }
}
