{
  "name": "glace-shell",
  "version": "0.1.0",
  "dependencies": {
    "fzf": "^0.5.2"
  },
  "scripts": {
    "dependencies": "ln -s ${ASTAL_GJS_PATH:-/usr/share/astal/gjs} ./node_modules/astal",
    "init": "ags types -d .; npm install",
    "dev": "ags run -d .",
    "dev:watch": "watchexec -r -w . -- ags run -d ."
  }
}
